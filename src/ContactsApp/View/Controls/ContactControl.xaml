<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Controls"
             xmlns:vm="clr-namespace:View.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Картинка -->
            <Grid Margin="0, 19, 0, 0">
                <Image Width="100" Height="100" VerticalAlignment="Top" Source="../new_contact.jpg"/>
            </Grid>

            <!-- Инфо -->
            <StackPanel Grid.Column="3" Grid.Row="0">
                <TextBlock Text="Name:" Height="16"/>
                <TextBox x:Name="NameTextBox" Text="{Binding SelectedContact.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0, 3, 0, 3" IsReadOnly="{Binding IsReadOnly}" />
                <TextBlock Text="Phone Number:" Height="16"/>
                <TextBox x:Name="PhoneNumberTextBox" Text="{Binding SelectedContact.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0, 3, 0, 3" IsReadOnly="{Binding IsReadOnly}" PreviewTextInput="PhoneNumberTextBox_PreviewTextInput" DataObject.Pasting="PhoneNumberTextBox_Pasting"/>
                <TextBlock Text="Email" Height="16"/>
                <TextBox x:Name="EmailTextBox" Text="{Binding SelectedContact.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0, 3, 0, 3" IsReadOnly="{Binding IsReadOnly}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>